apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hazelcast.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "hazelcast.name" . }}
    helm.sh/chart: {{ include "hazelcast.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "hazelcast.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
